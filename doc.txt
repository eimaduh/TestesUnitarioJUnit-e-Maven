1- COMANDOS:
   Compilar: compile;
   Testar: test;
   Empacotar: package;
   Limpar: clean; 

2- Testes;
   Apache Maven Archetype;
   Testes unitários com JUnit;
   Desenvolvendo teste com mockito;

3- PESQUISAR DEPOIS:
   pom.xml: mvnrepository.com; 
   Tipos de dependencia; 
   Transitividade escopo, dependencias opcionais e exclusions; 

4- TUTORIAL E EXEMPLOS A AJUSTAR/CRIAR:
   Exceptions; 
   Condicionais; 
   Assumptions; 
   Ordenacao; 
   Same; 
   Disabled; 
   import static; 

5- INICIANDO CLASSE USANDO MOCKITO:
   @ExtendWith(MockitoExtension.class) = criar um class mockito;
   @Mock = mocar um objeto;
   Stubbing(when, thenReturn, thenThrow);
   @Spy = espiar um objeto;
   Verify;
   @InjectMocks = moca a classe;
   @Captor = vai capturar o argumento;
   mockito-inline;

6- PARA SE APROFUNDAR:
   BDD Mockito;
   Integração Spring;


